import React, { Component } from 'react';

class Arts extends Component {
  render() {

    if(this.props.data){
    }

    return (
      <section>
      </section>
    );
  }
}

export default Arts;
